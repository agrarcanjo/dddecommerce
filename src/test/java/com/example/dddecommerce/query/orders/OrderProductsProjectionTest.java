package com.example.dddecommerce.query.orders;

import com.example.dddecommerce.restapi.dto.Order;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.UUID;

public class OrderProductsProjectionTest {

    private OrderProductsRepository orderProductsRepository;
    private OrderProductsProjection tested;
    @BeforeEach
    void setUp() {
//        orderProductsRepository = mock(OrderProductsRepository.class);
//        tested = new OrderProductsProjection(orderProductsRepository);
    }

    @Test
    void shouldCreateOrder_thenSaveIt() {
//        final Order order = new Order(UUID.randomUUID().toString(),"customer");
//
//        final UUID id = tested.on(order);
//
//        verify(orderProductsRepository).save(any(Order.class));
//        assertNotNull(id);
    }
}
